# Definition of Done — API Change

| Field | Value |
|---|---|
| **Work Type** | API Change |
| **Version** | 1.0 |
| **Last Updated** | 2025-01-09 |
| **Next Review** | 2025-04-09 |
| **Generated by** | DoD Builder v1.0 (Claude Sonnet 4.5 / Amazon Bedrock) |
| **Team** | Commerce Platform |
| **Organisation** | Acme Engineering |

---

## Code Quality

- [ ] **[L1] [ENG-4.1]** PR has minimum 2 approvals from engineers other than the author, including 1 from a senior engineer or tech lead.  
  **Verification:** GitHub PR approval count visible in PR status. Reviewers list shows roles.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.2]** All commits are signed with GPG or SSH key registered in GitHub.  
  **Verification:** GitHub commit list shows "Verified" badge on all commits.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.2-CONV]** All commit messages follow Conventional Commits format: type(scope): description.  
  **Verification:** GitHub Actions commitlint check passes in CI workflow.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.2-MERGE]** Branch merged to main using squash merge (no merge commits).  
  **Verification:** GitHub branch protection rules enforce squash merge. Merge button shows "Squash and merge" only.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.3-COMPLEXITY]** SonarQube quality gate passes with cyclomatic complexity ≤ 10 per method.  
  **Verification:** SonarQube check passes in GitHub Actions CI. Quality gate status visible in PR checks.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.3-LOC]** No method exceeds 50 lines of code (verified by SonarQube).  
  **Verification:** SonarQube quality gate passes in GitHub Actions CI with zero long method violations.  
  **Exception:** No exceptions.

- [ ] **[L1] [ENG-4.3-DEAD]** SonarQube reports zero dead code issues (unreachable or commented-out code removed).  
  **Verification:** SonarQube quality gate passes in GitHub Actions CI with zero dead code issues.  
  **Exception:** No exceptions.

- [ ] **[L2] [ENG-4.1-SEC]** PRs touching security-critical paths have additional approval from Security Champion.  
  **Verification:** Security Champion approval documented in PR comments or approvals list if auth/authz/payment/PII endpoints modified.  
  **Exception:** EM can grant exemption for non-security-critical API changes with written justification in PR.

---

## Test Coverage & Quality

- [ ] **[L1] [QA-2.1-COVERAGE]** JaCoCo reports 80%+ line coverage on all changed modules in CI.  
  **Verification:** JaCoCo coverage check passes in GitHub Actions CI. Coverage report shows 80%+ on changed files.  
  **Exception:** No exceptions.

- [ ] **[L1] [QA-2.1-COVERAGE]** JaCoCo reports 75%+ branch coverage on all changed modules in CI.  
  **Verification:** JaCoCo coverage check passes in GitHub Actions CI. Coverage report shows 75%+ branch coverage on changed files.  
  **Exception:** No exceptions.

- [ ] **[L1] [QA-2.1-COVERAGE]** CI coverage check passes with no coverage regression on existing covered code.  
  **Verification:** JaCoCo diff check in GitHub Actions shows no decrease in coverage percentage for any existing file.  
  **Exception:** No exceptions.

- [ ] **[L2] [QA-2.2-API]** Unit tests in JUnit 5 present and passing for API endpoint logic.  
  **Verification:** JUnit 5 test suite runs in GitHub Actions CI. Test report shows passing tests for controller/service layer of modified endpoints.  
  **Exception:** EM can grant one-sprint exemption with written justification for trivial pass-through endpoints.

- [ ] **[L2] [QA-2.2-API]** Integration tests in JUnit 5 present and passing for API contract and data flow.  
  **Verification:** Integration test suite runs in GitHub Actions CI. Tests verify request/response format, status codes, and database interactions.  
  **Exception:** EM can grant one-sprint exemption with written justification if endpoint has no external dependencies.

- [ ] **[L2] [From INC-002]** API contract test includes all nullable fields documented in the OpenAPI spec.  
  **Verification:** Integration test suite includes test cases sending null values for all nullable fields in request/response. Tests verify system handles nulls without 500 errors.  
  **Exception:** No exceptions. This criterion prevents recurrence of P2 incident INC-002.

- [ ] **[L2] [QA-2.4-PERF]** k6 load test present in CI for endpoints expected to handle > 1000 RPS.  
  **Verification:** k6 test script exists in tests/load/ directory. GitHub Actions workflow runs k6 test for high-throughput endpoints.  
  **Exception:** Not required if endpoint is documented as < 1000 RPS expected load. Exception must be noted in PR description.

- [ ] **[L2] [QA-2.4-PERF]** k6 test reports P95 response time < 500ms under normal load.  
  **Verification:** k6 test output in CI shows P95 latency < 500ms. Test runs with load profile matching expected production traffic.  
  **Exception:** Not required if TEST-07 exemption applies. Exception must be noted in PR description.

---

## Security

- [ ] **[L1] [SEC-3.1-SNYK]** Snyk scan shows 0 Critical or High severity findings in GitHub Actions CI.  
  **Verification:** Snyk security check passes in GitHub Actions workflow. Snyk report shows no Critical/High findings.  
  **Exception:** No exceptions. Findings must be fixed or have a CISO-accepted risk waiver attached to PR.

- [ ] **[L1] [SEC-3.1-SNYK]** Medium severity findings from Snyk have tracked Jira tickets linked in PR description.  
  **Verification:** PR description contains Jira ticket links for all Medium findings. Tickets are in backlog with target resolution sprint.  
  **Exception:** No exceptions.

- [ ] **[L1] [SEC-3.2-SECRETS]** Gitleaks scan passes in GitHub Actions CI with zero secrets detected.  
  **Verification:** Gitleaks check passes in GitHub Actions workflow. No secrets, tokens, API keys, or passwords detected in code or config files.  
  **Exception:** No exceptions.

- [ ] **[L1] [SEC-3.3-DEPS]** mvn dependency:check runs in GitHub Actions CI and reports zero Critical CVE findings.  
  **Verification:** Maven dependency check passes in GitHub Actions. OWASP Dependency Check report shows no Critical vulnerabilities.  
  **Exception:** No exceptions. Dependencies with Critical CVEs must be upgraded or removed before merge.

- [ ] **[L1] [SEC-3.3-DEPS]** All new third-party dependencies are listed in approved dependency registry.  
  **Verification:** PR description documents new dependencies added. Dependencies match entries in approved registry (docs/approved-dependencies.yaml).  
  **Exception:** No exceptions. New dependencies require Security Champion review before registry addition.

- [ ] **[L1] [SEC-4.2-SECRETS-MGMT]** All secrets stored in AWS Secrets Manager or GitHub Actions secrets (documented in PR if new secrets added).  
  **Verification:** PR description documents location of any new secrets. Application code references AWS Secrets Manager or GitHub secrets, not hardcoded values.  
  **Exception:** No exceptions.

---

## Documentation

- [ ] **[L2] [ENG-6.1-API]** OpenAPI specification file (api/openapi.yaml) updated to reflect API changes.  
  **Verification:** api/openapi.yaml file modified in PR. OpenAPI spec includes new/modified endpoints, request/response schemas, and error responses.  
  **Exception:** EM can grant one-sprint exemption with written justification if spec update is blocked by tooling issue. Tech debt ticket required.

- [ ] **[L2] [ENG-6.1-API]** CHANGELOG.md updated with entry under [Unreleased] section.  
  **Verification:** CHANGELOG.md file modified in PR. Entry describes API change under [Unreleased] header with type (Added/Changed/Deprecated/Removed).  
  **Exception:** EM can grant one-sprint exemption with written justification for internal-only API changes. Tech debt ticket required.

- [ ] **[L2] [ENG-6.1-API]** API diff generated and attached to pull request.  
  **Verification:** PR description or comments contain API diff output (openapi-diff or Swagger diff tool). Diff highlights breaking changes, new endpoints, and schema modifications.  
  **Exception:** EM can grant one-sprint exemption with written justification for non-public API changes. Tech debt ticket required.

- [ ] **[L3] [TEAM-KAFKA]** If API change involves Kafka event schema modification, event schema registry updated with new version.  
  **Verification:** Kafka schema registry (Confluent Schema Registry) shows new schema version registered. PR description documents schema compatibility mode (BACKWARD/FORWARD/FULL).  
  **Exception:** Tech Lead approval required. Reviewed quarterly. Not applicable if change does not touch Kafka events.

---

## CI / Deployment Gate

- [ ] **[L1] [CI-UNIVERSAL]** All GitHub Actions checks pass (build, test,